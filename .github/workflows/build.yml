name: Build

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: true

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: |
            InfuseSync/packages.*.lock.json
            InfuseSync.Tests/packages.lock.json

      - name: Restore dependencies
        run: dotnet restore InfuseSync.sln --locked-mode

      - name: Build
        run: dotnet build InfuseSync.sln --configuration Release --no-restore -p:ContinuousIntegrationBuild=true

      - name: Test
        run: dotnet test InfuseSync.Tests/InfuseSync.Tests.csproj --configuration Release --no-build --no-restore
